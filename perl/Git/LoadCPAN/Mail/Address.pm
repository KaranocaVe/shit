package shit::LoadCPAN::Mail::Address;
use 5.008001;
use strict;
use warnings $ENV{shit_PERL_FATAL_WARNINGS} ? qw(FATAL all) : ();
use shit::LoadCPAN (
	module => 'Mail::Address',
	import => 0,
);

1;
